import 'package:flutter/material.dart';

class TirmidhiPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Tirmidhi Page'),
      ),
      body: Center(
        child: Text('Tirmidhi Page'),
      ),
    );
  }
}