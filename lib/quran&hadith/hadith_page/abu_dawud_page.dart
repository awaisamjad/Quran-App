import 'package:flutter/material.dart';

class AbuDawudPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Abu Dawud Page'),
      ),
      body: Center(
        child: Text('Abu Dawud Page'),
      ),
    );
  }
}