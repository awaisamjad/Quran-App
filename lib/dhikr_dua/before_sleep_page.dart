import 'package:flutter/material.dart';

class BeforeSleepPage extends StatelessWidget{
  BeforeSleepPage();
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title:Text('Before Sleep'),
      ),
      body: Text('Before Sleep'),
    );
  }
}