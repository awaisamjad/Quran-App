name: quran_app
description: A Quran App focused on recitations.
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  quran: ^1.2.2
  record: ^4.4.4
  audioplayers: ^4.1.0
  hadith: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  file_manager: ^1.0.0

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/verses/

